/** Frontend constants. Match backend study_config where noted. */

/** Leeches threshold (match backend leech_lapses_threshold). */
export const LEECH_LAPSES_THRESHOLD = 8;

export const SEARCH_RESULTS_LIMIT = 80;

export const LIST_CARDS_LIMIT = 200;
export const METRICS_SUMMARY_LIMIT = 200;
